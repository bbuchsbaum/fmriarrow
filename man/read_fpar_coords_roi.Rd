% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_fpar_coords_roi.R
\name{read_fpar_coords_roi}
\alias{read_fpar_coords_roi}
\title{Read voxel data for a coordinate-based ROI}
\usage{
read_fpar_coords_roi(
  parquet_path,
  x_range,
  y_range,
  z_range,
  columns = NULL,
  exact = TRUE,
  max_coord_bits = 10
)
}
\arguments{
\item{parquet_path}{Path to the Parquet file.}

\item{x_range}{Vector of length 1 or 2 giving the x-coordinate range (0-based, inclusive).}

\item{y_range}{Vector of length 1 or 2 giving the y-coordinate range (0-based, inclusive).}

\item{z_range}{Vector of length 1 or 2 giving the z-coordinate range (0-based, inclusive).}

\item{columns}{Optional character vector of columns to return. If `NULL`, all
columns are returned.}

\item{exact}{If `TRUE` (default), coordinates are filtered exactly after an
initial Z-index range search. When `FALSE`, only the coarse Z-index
filtering is applied which may include voxels outside the requested cuboid
but can be faster for large ROIs.}

\item{max_coord_bits}{Maximum number of bits per coordinate (default: 10).}
}
\value{
An Arrow Table containing the filtered rows.
}
\description{
Queries a Parquet file produced by `neurovec_to_fpar()` for voxels
within the specified coordinate ranges.
}
